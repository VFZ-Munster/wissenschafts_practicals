# Logistic regression recap

Open `logistic_regression_10min_recap.md` with a Markdown viewer, or present it using Marp.

The figures are stored in the `figures` folder and are referenced with relative paths.
